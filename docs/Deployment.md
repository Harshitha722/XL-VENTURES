# Deployment

Recommended production targets:

- Frontend: Vercel
- Backend: Railway
- Database: Supabase PostgreSQL with pgvector
- Vector database: Qdrant Cloud
- Redis: Upstash

Configure environment variables from `backend/.env.example` and `frontend/.env.example`. Docker Compose is provided for local integration.
